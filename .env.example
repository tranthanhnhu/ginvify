# Public site origin (sitemap, metadata, canonicals)
NEXT_PUBLIC_SITE_URL=https://ginvify.com

# Separate hosting repo folder inside this project (NOT out/).
# Receives rsync from out/ after build. Has its own .git — gitignored by parent.
# Relative paths resolve from the project root.
STATIC_DEPLOY_DIR=outhostinger
